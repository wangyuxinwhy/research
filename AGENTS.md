# AI 代理指南

这个文档为在本仓库中工作的 AI 编码代理提供指导。

## 快速开始

1. **创建项目文件夹**：使用小写字母和连字符的命名（如 `redis-benchmark`）
2. **创建 notes.md**：在工作过程中记录你的尝试、学习和决策
3. **最后构建 README.md**：详细的研究报告（参考 [PROJECT_TEMPLATE.md](PROJECT_TEMPLATE.md)）
4. **最终提交应该只包含**：
   - `notes.md` 和 `README.md` 文件
   - 你编写的代码
   - 如果修改了第三方代码，保存 `git diff` 的输出为文件，但**不要**包含完整的第三方代码副本
   - 适当的二进制文件（如果小于 2MB）
5. **不要创建 `_summary.md`**：这个文件会在提交后自动生成

## 项目原则

### 1. 代码研究优先

本仓库的目的是进行**代码研究**——通过编写和执行代码来回答技术问题。不要只是理论分析，要：

- 编写可执行的代码来验证假设
- 运行实验来测试不同的方案
- 用实际的性能数据支持结论
- 创建可重现的演示

### 2. 完整且独立

每个研究项目应该：

- 在独立的目录中工作（`project-name/`）
- 包含所有必要的依赖声明
- 包含运行和测试的说明
- 可以被其他人独立复现

### 3. 记录过程

- 在 README.md 中记录研究过程和发现
- 包含你尝试的不同方法（成功的和失败的）
- 解释为什么选择某个方案
- 记录性能数据、测试结果和关键决策

### 4. 可执行性

- 代码必须能够实际运行
- 包含测试来验证功能
- 提供示例用法
- 如果需要外部服务，提供模拟数据或说明

## 项目结构指南

```
project-name/
├── README.md              # 详细的研究报告
├── requirements.txt       # Python 依赖
│   或 package.json        # Node.js 依赖
├── src/                   # 源代码
│   └── main.py           # 主要实现
├── tests/                 # 测试代码
│   └── test_main.py
├── examples/              # 使用示例
│   └── demo.py
├── results/               # 实验结果
│   ├── data.json
│   └── charts/
└── docs/                  # 额外文档
```

## README.md 必须包含

每个项目的 README 应该包含：

1. **项目目标**：明确说明要回答什么问题或验证什么假设
2. **方法**：描述你使用的技术和方法
3. **实现细节**：关键代码片段和解释
4. **实验结果**：数据、图表、性能指标
5. **发现和结论**：你学到了什么？有什么意外发现？
6. **使用说明**：如何运行代码和复现结果
7. **参考资料**：相关文档、文章、项目

## 网络访问

你可以自由地：

- 安装任何需要的依赖包
- 从网络获取数据
- 下载外部资源
- 使用 API 服务（注意不要提交密钥）

## 实验和基准测试

当进行性能比较时：

- 测试多个方案（至少 3 个）
- 使用多种数据集大小
- 重复测试以获得统计意义
- 生成可视化图表
- 记录测试环境（Python 版本、操作系统等）

## 质量标准

- **代码必须能运行**：不要提交未测试的代码
- **测试必须通过**：包含并运行测试
- **文档必须清晰**：其他人应该能理解你做了什么
- **结果必须可验证**：提供足够的信息让他人复现

## 构建在之前的工作上

可以参考和使用本仓库中其他项目的代码：

- 查看其他项目寻找灵感
- 重用和改进已有的代码
- 比较不同的实现方法
- 在 README 中引用相关项目

## 常见研究主题

- 性能基准测试
- 不同库或工具的比较
- 新技术的可行性研究
- 算法实现和优化
- 数据处理和分析
- API 集成和测试
- WebAssembly 和跨平台技术
- 机器学习实验

## 注意事项

- **不要提交敏感信息**：API 密钥、密码、个人数据
- **尊重许可证**：如果使用他人的代码，遵守许可证
- **明确标注来源**：引用的代码和数据要注明出处
- **保持专注**：每个项目探索一个明确的主题

## 报告格式

使用 Markdown，包含：

- 清晰的标题层次
- 代码块使用语法高亮
- 表格展示数据对比
- 图片和图表（保存在项目目录中）
- 链接到相关资源

## 示例提示词模式

好的研究任务通常是这样的：

> "比较 X、Y、Z 三个库在处理大规模数据时的性能。下载或生成测试数据，运行基准测试，生成性能图表，并在 README 中详细报告发现。"

> "探索是否可以在 WebAssembly 中运行 [某个库]。如果需要，编译必要的二进制文件，编写测试验证功能，记录遇到的问题和解决方案。"

> "从 [数据源] 获取数据，使用机器学习方法解决 [问题]，尝试至少 3 种不同的模型，比较结果，包含可视化和详细分析。"

## 完成标准

项目完成时应该：

- ✅ 代码可以运行
- ✅ 测试通过
- ✅ README 完整且清晰
- ✅ 包含可验证的结果
- ✅ 回答了研究问题
- ✅ 提供了有价值的见解
