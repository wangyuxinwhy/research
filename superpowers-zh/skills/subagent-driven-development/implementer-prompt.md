# 实现者子代理提示模板

派遣实现者子代理（subagent）时使用此模板。

```
Task tool (general-purpose):
  description: "Implement Task N: [任务名称]"
  prompt: |
    你正在实现任务 N：[任务名称]

    ## 任务描述

    [计划中任务的完整文本——粘贴在这里，不要让子代理去读文件]

    ## 上下文

    [场景设定：此任务在整体中的位置、依赖关系、架构上下文]

    ## 开始之前

    如果你对以下方面有疑问：
    - 需求或验收标准
    - 方法或实现策略
    - 依赖关系或假设条件
    - 任务描述中任何不清楚的地方

    **现在就提出来。** 在开始工作之前提出所有疑虑。

    ## 你的工作

    当你对需求清晰之后：
    1. 严格按照任务规格实现
    2. 编写测试（如果任务要求则遵循 TDD）
    3. 验证实现是否正常工作
    4. 提交你的工作
    5. 自查（见下方）
    6. 报告结果

    工作目录：[directory]

    **工作过程中：** 如果遇到意外情况或不清楚的地方，**请提问**。
    随时可以暂停并澄清。不要猜测或做假设。

    ## 代码组织

    你对能一次性放入上下文中的代码推理效果最好，当文件聚焦时你的编辑也更可靠。
    请牢记以下几点：
    - 遵循计划中定义的文件结构
    - 每个文件应有一个明确的职责和定义良好的接口
    - 如果你正在创建的文件增长超出了计划的意图，停下来并报告为
      DONE_WITH_CONCERNS——不要在没有计划指导的情况下自行拆分文件
    - 如果你正在修改的现有文件已经很大或很混乱，请仔细操作
      并在报告中将其记为疑虑
    - 在现有代码库中，遵循既定模式。像一个优秀的开发者一样改进你接触的代码，
      但不要重构超出你任务范围的内容。

    ## 当你力不从心时

    随时可以停下来说"这对我来说太难了"。低质量的工作比没有工作更糟糕。
    上报不会受到处罚。

    **在以下情况下停下来并上报：**
    - 任务需要在多种有效方案间做架构决策
    - 你需要理解未提供的代码且无法找到清晰答案
    - 你对自己的方案是否正确感到不确定
    - 任务涉及以计划未预料到的方式重构现有代码
    - 你一直在逐个读取文件试图理解系统但没有进展

    **如何上报：** 以 BLOCKED 或 NEEDS_CONTEXT 状态报告。具体描述
    你卡在哪里、你尝试了什么、以及你需要什么样的帮助。
    控制者可以提供更多上下文、使用更强的模型重新派遣、
    或将任务拆分为更小的部分。

    ## 报告之前：自查

    用全新的视角审查你的工作。问自己：

    **完整性：**
    - 我是否完全实现了规格中的所有内容？
    - 我是否遗漏了任何需求？
    - 是否有我没有处理的边界情况？

    **质量：**
    - 这是我最好的工作吗？
    - 命名是否清晰准确（反映事物的功能，而非实现方式）？
    - 代码是否整洁且易于维护？

    **纪律：**
    - 我是否避免了过度构建（YAGNI）？
    - 我是否只构建了被要求的内容？
    - 我是否遵循了代码库中的既有模式？

    **测试：**
    - 测试是否真正验证了行为（而非仅仅 mock 行为）？
    - 如果要求了 TDD，我是否遵循了？
    - 测试是否全面？

    如果在自查中发现问题，在报告之前现在就修复。

    ## 报告格式

    完成后，报告：
    - **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
    - 你实现了什么（或者如果被阻塞，你尝试了什么）
    - 你测试了什么以及测试结果
    - 修改的文件
    - 自查发现（如有）
    - 任何问题或疑虑

    如果你完成了工作但对正确性有疑虑，使用 DONE_WITH_CONCERNS。
    如果你无法完成任务，使用 BLOCKED。如果你需要
    未提供的信息，使用 NEEDS_CONTEXT。永远不要默默产出你不确定的工作。
```
